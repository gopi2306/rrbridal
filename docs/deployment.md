# Deployment

## Central backend (NestJS + MongoDB)
- Recommended: run API as a container (Docker) and MongoDB as either:\n  - **MongoDB Atlas** (managed), or\n  - a dedicated VM/container (self-hosted)\n+
Environment:\n- `MONGO_URI`\n- `PORT`\n- `JWT_SECRET` (when JWT is enabled)\n- `RAZORPAY_KEY_ID`, `RAZORPAY_KEY_SECRET`\n\nRun:\n- `npm install`\n- `npm run build`\n- `npm run start`\n\n## Store billing (WPF + local MongoDB)\n- Install the WPF app on each store PC.\n- Install and run **MongoDB Community Server** as a Windows service on each store PC.\n- Configure environment variables (or app settings UI in next iteration):\n  - `STORE_MONGO_URI` (default: `mongodb://localhost:27017/rr_bridal_store`)\n  - `CENTRAL_API_BASE` (default: `http://localhost:3000`)\n  - `STORE_ID`, `DEVICE_ID`\n+\n## MongoDB local service provisioning (store)\n- Create a script in `store-billing-wpf/scripts/` to:\n  - install MongoDB if missing\n  - create data directory\n  - register/start `mongod` Windows service\n*** End Patch"}]} 򐔟4? tool expects FREEFORM not json. We'll fix. Let's call ApplyPatch proper. }Oops. Need correct tool call. We'll reissue ApplyPatch. We'll ignore previous invalid? It's tool call error; handle. We'll now apply. It's in comment; Actually tool didn't run due to JSON; We'll call properly.  Nothing else. Let's do. }  elif. We'll do ApplyPatch again. } 
