<UserControl x:Class="RRBridal.StoreBilling.App.Views.QuotationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:RRBridal.StoreBilling.App.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{StaticResource BrushBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Padding="14,12" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Quotation" Style="{StaticResource TextPageTitle}" Foreground="{StaticResource BrushAccent}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <Border Background="{StaticResource BrushSidebarTint}" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                            <TextBlock Text="{Binding QuotationNo, StringFormat=No: {0}}" FontSize="12" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource BrushTextMuted}" FontSize="12"
                                   VerticalAlignment="Center" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="← Back to list" Style="{StaticResource OutlineButton}" MinWidth="110" Height="36" Margin="0,0,8,0"
                            Command="{Binding BackToListCommand}"/>
                    <Button Content="New (F2)" Style="{StaticResource OutlineButton}" MinWidth="96" Height="36" Margin="0,0,8,0"
                            Command="{Binding NewQuotationCommand}"/>
                    <Button Content="Print" Style="{StaticResource OutlineButton}" MinWidth="88" Height="36" Margin="0,0,8,0"
                            Command="{Binding PrintQuotationCommand}"/>
                    <Button Content="Save quotation" Style="{StaticResource PrimaryButton}" MinWidth="140" Height="36"
                            Command="{Binding SaveQuotationCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <views:BillingView Grid.Row="1" DataContext="{Binding Editor}"/>
    </Grid>
</UserControl>
