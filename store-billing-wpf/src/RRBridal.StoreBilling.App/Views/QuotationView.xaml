<UserControl x:Class="RRBridal.StoreBilling.App.Views.QuotationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:RRBridal.StoreBilling.App.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{StaticResource BrushBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Padding="12,10" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Quotation" Style="{StaticResource TextPageTitle}" Foreground="{StaticResource BrushAccent}"/>
                    <TextBlock Text="{Binding QuotationNo, StringFormat=Quotation no: {0}}" Margin="0,4,0,0"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="New" Style="{StaticResource OutlineButton}" Width="90" Margin="0,0,8,0"
                            Command="{Binding NewQuotationCommand}"/>
                    <Button Content="Print" Style="{StaticResource OutlineButton}" Width="90" Margin="0,0,8,0"
                            Command="{Binding PrintQuotationCommand}"/>
                    <Button Content="Save quotation" Style="{StaticResource PrimaryButton}" MinWidth="140"
                            Command="{Binding SaveQuotationCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <views:BillingView Grid.Row="1" DataContext="{Binding Editor}"/>
    </Grid>
</UserControl>
