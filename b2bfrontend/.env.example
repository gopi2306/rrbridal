VITE_B2B_API_URL=/api
# Copy to .env.local for local overrides (optional).
#
# Coming soon gate — enabled automatically in production builds via .env.production.
# Local dev (npm run dev) shows the full site unless you set this to true.
VITE_COMING_SOON=false
